- python 3 + Qt application
- photo viewer / metadata viewer + editor
- Grid view for photo navigation : thumbnais + optionally full resolution in fullscreen 
- panel for exif : pyexitool used for reading

- uv is used for python project management : deps defined in `pyproject.toml`
- to run a python file : use `uv run python ...`
- write a plan for the following feature to complete the python / pyqt application piqopiqo:
- write an ACTION_10.md file with the detail of tasks
- try to write them separately + in order so I can task you in sequence if needed
- if something does not make sense, tell it : I haven't worked on this in a while

- cache by origin folder : currently single folder : if same name, will overwrite : so hash of the folder path (possibly 32 characters) (if noved will be a different cache)
- create subfolder thumb (in case there is other data to be stored in there)
- since there can be photos from multiple folders ("Recursively scans a folder for images) there can be multiple folers created when openin a folder (for the subfolders). Create different cache for each of the folder next to each other (not hierarchical even if subfolders)

- add a menu item to : regenerate the thumbnail cache

- Add a menu Item to open folder : it will as if the program was opened with that folder as parameter
- Make it possible to open the program without folder. In that case : if a folder was opened (as parameter from a previous run or using the menu item), save the opened folder in a JSON file in a support folder (eg Application Support in macos, AppData in Windows, .config in Linux => write a function to determine the location or reuse one of the exiting libraries already installed ; do not install a new one ; for now only macos is important ). If no folder passed at opening : open the folder in that support JSON file. If no folder was passed and no JSON file : open an empty application : with a panel with an Open folder button in the center. When a folder has been selected, iitialize the app like when a folder is passed.

- Create a new file for the component for that feature :
- On the right side : above the EXIF panel : add another panel (the relative size can be changed with a splitter). That panel will be used for editing data about the photos. It is different from the EXIF (it will not be saved inside the photo file, although initial value can come from the EXIF inside the photo).
- when a photo is selected in the grid : its editable data becomes visible in the panel. Focus still remains on the photo until a field is selected for editing.
- The data will be stored in a SQL Lite database in the same folder as the cache for a photo folder : will be named db (next to the thumb folder). There will be a db for all physical folders (like there is a a thumb cache)
- Only create the db file after a the first time data has been edited. And only for the photos with actual edited data
- Save is done by pressing enter while editing the field. Focus changes back to the photo in the grid
- Key "Escape" while editing resets to the former value of the field.
- Make tab useable to switch from fields. If the field had been edited when switching : it should be considered saved (same as Enter) and focus passes to the next field. The tab goes around : returns to the first field if from the last
- all the fields can have a cmd-z for undo.

- The data to be edited for a photo will be : title, description, lat, lon, keywords, time, status (label)
- there should be a header string followed by the field (mostly a text field but can be a comobox for the label)
- If there is corresponding data in the EXIF of the image (in the ACTION_10.md write the corresponding exiftool field names) : it will be recopied in the fields to be edited in the GUI as an initial value. However : if not edited by the user and saved (with enter in the field or tab), the db file or the line in the DB about the photo will not be created (until the first time this happens => then the files is created or the data for all the editable fields for the specific photo will be saved : even if that piece of data not edited)
- title is a single line : Return will save the data. Cmd-Enter will do nothing. If title is long, the field in the GUI can be multiple lines (but the content will not have newlines). If pasted from somewhere else : newlines are removed and replace by space.
- description can have new lines. Newlines are added with cmd-Enter in macos (or alt enter in other platforms)
- Add to the config : an option to display or not the editable description and make use of it.
- Also add to the config : an option for the max size of title  And a 2nd option for the max size of description. 128 as default.
- keywords are a list of keywords : single line. If does not fit in a single line : the field in the GUI can expand (like the title field). It could be arbitrary lenght.
- lat lon : use decimal format NOT the EXIF gps format, although convert from the EXIF (see /Users/guilhem/Documents/projects/github/gpx2exif/gpx2exif/gpx2exif.py for the opposite : ie convert from decimal to EXIF). Validate the lat lon, make sure they are valid decimal numbers.
- Display the time DateTimeOriginal from the EXIF. Also validate the edition.
- status (label) is the http://ns.adobe.com/xap/1.0/ Label from XML (not sure how exiftool refers to it) : it is a predefined string value. The values are listed in the config.py file : add a variable there : an array with string and color hex (or name). The field is a combobox. The intial value is No Label. Changing this value to other sets the value (no need to enter) : if set to No Label : the value of the field is cleared (set to null)

- if multiple photos selected : first, a value is determined for each photo. If there are multiple values for the field : a <Multiple Values> string is displayed as the value of the field. If focusing for editing : the <Multiple Values> is erased right away and the field becomes empty. If a new value is entered (and confirmed with tab or enter), all the photos have that field set to that value. cmd z  returns to the empty value not <Multiple Value>  : only Escape resets the field : its display is <Multiple Value> and the data for each photo is NOT CHANGED (including not set to <Multiple Values>).
- if the exif does not have the specific data, leave empty as an initial value (in the db will be null) : if data entered make sure it is validated. If text is entered and cleared : set to null

- add a small panel on top of the main panel : filter
- it will be for filtering on folders (only if detected that recursively scanning the folder passed as arg or opened through the application has loaded photos from multiple folders : so not necessarily multiple folders just multiple folders with photos inside). In that caase : create a combobox wih the list of detected folders (from where a photo has been found). Only the last part (not the full path). Add a check box next to it so can be deactivated easily. When selecting an item in the list : the checkbox is automatically checked (so no need for unnecessary double interaction)

Draw the overlay with the zoom factor : at the top of the scren (with some margin).  Not at the center. Only 100%,  200%, 400%, 800% should ever be shown as descrbed below.

This will be the zoom sequence (for interaction and overlay) : 
- the base view : if the photo is big enough it will be a zoomed out from 100% (so smaller than 100%). If it is small : that base view should already be 100% : It should not be any bigger factor (ie zoome in already). The base view is the same as now.
- 100% (special case) : the 100% is the same as now : it corresponds to 1 pixel in the image = 1 pixel in the macos rendering buffer
- 200 %, 400%, 800% : 1 pixel in the image is 2, 4, 8 pixels in the macos rendering buffer.

There is no longer any intermediate sequence between the base view and 100%. Remove the ZOOM_MAX and ZOOM_FACTOR from the config : they will no loger be used. There also won't be any intermediate zoom between 100% and the other zooms (only the 2, 4, 8 factors)

2 methods of mouse interaction with the zoom : left click OR wheel. For the click, when panning (ie click and drag), the click behaviour descibed below should not happen. It is a different kind of interaction.

click : 
assuming the image is large enough : (ie base view is not 100%) : in base view, a click on the photo zooms to 100% directly (even if the photo is large : there is no intermediate state as mentioned above). The center of the zoom is just like with the wheel : the mouse pointer remains at the same position in both base view and 100%
    When in 100% or beyond (if the wheel was used), a click goes back to the base view ie if zoomed in : it completley unzooms.
If the image is small : the base view will already be at 100%. In that case : a click does nothing (unless the wheel has been used to zoom 2x or more : then it completely unzooms to the base view ie 100%).

Wheel : 
while in the base view : a wheel turn (like now, using the ZOOM_WHEEL_SENSITIVITY)  in the zoom in direction will zoom to 100%. Other wheel turns will zoom to 200 %, 400%, 800%. If the image is small and the base view is already 100% : it will go directly to  200 %, 400%, 800% (it will not go to a state 100% different from the base view ie the view should move anyway)
in Zoom out direction : It will go in the different direction. It will never go beyond the base view. After 100%, if the image is big enough, the view will go back to the base view. If small image, it will stay there.


Use enums to know which state we are (5 states in all). Also track if the base view is already 100% for the image (ie "small image"). Use that state for the procesing + zoom overlay. Add to that state if not enough (If i forgot something)

Correct _should_show_zoom_overlay : 
the initial view that shows is the base view. The zoom overlay should never show in that view. This includes initial view + zoom out to get back to that view. 
The zoom overlay for 100% should not be shown when zooming in : It should only show when getting back to 100% (from a further zoom) 
If a small image and the base view is already 100% : it should show when going back to it from a further zoom (so different regarding the 1st point for small images)
There is no difference in the overlay for click or wheel interaction (it depends on the zoom factor + if small image).
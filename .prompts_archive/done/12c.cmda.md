- in the photo grid : Add a shortcut cmd-A (or ctrl-a in Qt) : when pressed : all the currently visible images (so after filter has been applied) are selected

- After filtering is applied : the images that were selected in the grid before the filter must still be selected after (only those to which the filter is applied ; if filtered out, item in data model must be unselected)
- in the grid : if clicking outside the grid item (on an empty space) : the selection must be cleared
- the cmd-A shortcut must also apply : not just when the grid is in focus : but the rest of the application : ie application wide shortcut

When setting keywords from metadata edit panel : when you validate and write to the db (after Enter or leave field or return from keyword tree), also clean the fields : remove the empty keywords (, , or ,,) => empty keywords should not be in db + format ie space after the separator ", " then next keyword + remove duplicates (first in the list takes precedence)
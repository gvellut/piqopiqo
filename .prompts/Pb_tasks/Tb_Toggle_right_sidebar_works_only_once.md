When I use the shortcut Toggle Right Sidebar : 
If I select an iten on the photo grid : then the first Toggle Right Sidebar works (to hide or show the sidebar). A second doesn't. I need to click again on the grid so the shortcut will work again (to hide or show the sidebar)
However if I first click on the sidebars (either  metadata edit panel or exif panel), then I can use the shortcut multiple times in the row (to hide and show the sidebar).
The shortcut must work with photogrid selected multiple times in a row.
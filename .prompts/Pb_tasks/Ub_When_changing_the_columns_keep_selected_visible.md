when changing the number of columns inf hte grid with the buttons : keep the selected visible. 
Use the same scheme as when the filtering changes (keep selected or last selected if multiple visible ; bias towards keeping on the same row number on the visible rows)

COLLAPSE_TO_LAST_SELECTED : when the shortcut is used : on top of only selected the last : make it visible if not already the case (if already visible nothing to do)
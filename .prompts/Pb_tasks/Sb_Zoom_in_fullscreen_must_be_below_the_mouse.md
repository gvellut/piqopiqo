The zoom in fullscreen using the click or the mouse wheel must keep the pixel under the mouse pointer after the zoom.
There is no exception.
Currently it works fine for photos in landscape 3:2 that are bigger than fullscreen in all dimensions at 100% (from base view : kept inside the screen, to 100%). But for vertical images : that are still not wide enough to cover the full width of the screen at 100% : the zoom from base view to 100% does not keep the pixel under the mouse pointer after the zoom.
Allow extra space based on current image position after (currently done).

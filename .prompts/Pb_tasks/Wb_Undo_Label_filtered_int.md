
 In photo grid : When applying a label to am image, and the new label is not filtered in (while the old one was).  It disappears when the label is applied : OK. But if I do Undo Label, the image is not displayed back. Needs to change filter for it to show.
=> Iconsistant : sometimes I see the issue but not always.


Also Undo Label should also deal with the label applied while in fullscreen : it must participate in the undo / redo work, like the grid. currently not the case : The Undo Label menu is grayed out and inactive. Or it can still indicate Redo (for a previous undo /redo operation in the grid I suppose) Or even Undo but it applies to another photo (very confusing). Not correct. So If I apply label to a photo while in fullscreen. If I exit and  the Undo Label must be active and the last label applied while in fullscreen must be reverted.

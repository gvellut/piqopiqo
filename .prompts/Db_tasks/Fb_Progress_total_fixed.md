Copy SD : 
The number of copies "/ <total>" :  make the / <total> position constant : when the number of copies updates: it moves the total left and right (since the characters that change do not have the same width). The /total should not move. Do this : Align to right (right side). also make the count above the progress bar instead of under (same line as Copying Files in gpx2exif). Remove the space formerly used for that label below the progress bar.

Check also with flickr_upload for upload, make public, reset dates : if same behaviour. Make the same fix : align to right + above the progress bar. On the right side and same line as the text on the left. For check (number of tries): align to the right also and do the same.

Always put the counter on the right side above the progress bar . Same line as the status on the left (no additional vertical space above the progress bar compared to now).

Unrelated :
Copy SD :

Add "between:...": all the dates between the dates (exclusive of both sides), separated with -. If missing first side : equivalent of last one before second side : between:-20250415 => between the last (on disk : that part is dynamic like with since:last) before 20250415 and 20250415 (again exclusive of both sides ): to be used if copying from old SD after change of card (but new card already copied). It takes into account the SD name (like since:last) to determine the last.
Make synonyms
s : synonym of since
b: synonyme of between

Criteria : filter removed + selected or keep current in view in grid => try to keep identical the line (on screen) where the selected was, as much as possible. Currently : can be moved to top of the grid view from bottom. Jarring,  Try as much as possible to keep its line in the view
So if current view of grid has 3 rows : relevant picture before filter update was 1 => it should stay on 1 not move to 3.
Relevant picture : not sure what the criteria is to keep in view : if selected in view : relevant picture will be the last selected (already kept) or if not in view, one of the selected in view (say first in the sorted list). No need to work on ALL the selected images. If no selected in view : keep the relevant picture as for keep visibility. Use the same criteria (to keep visibilty => apply to keep the same row)
If not possible because not enough photo to keep the lines : OK. 


Also : unrelated : When opening a new folder. Reset the view : move to the top (very first photo) + reset filters (already done I think).
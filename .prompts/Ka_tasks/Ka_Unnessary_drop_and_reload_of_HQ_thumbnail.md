Some issue with the thumbnail loading : I have a photo selected and visible (so HQ thumbnail is loaded) But I do a filter in which that photo is included, the image changes position in the grid (normal) and stays visible but the HQ Thumbnail seems to be temporarily replaced by the low res thumbnail before being switched again. In the case of "visible images that stays visible after a filter  (or sort)", there should be no switch of the thumbnail. It should remain as it is. Just moved in the grid.

check where it comes from and fix.

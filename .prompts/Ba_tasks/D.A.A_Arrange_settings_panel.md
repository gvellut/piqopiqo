- The settings menu should be Settings... on macos. It can stay Preferences in other platforms
- The settings with Path should be aligned to the left with the field for the path + Browse button taking the rest of the width. Currently cut. 
- External / Workflow : The value for both fields should be a Path to an app : ie /Applications/Gimp.app. Not just a string. There should be a browse button (same as other Path) . The browsing start on the /Applications (if nothing chosen yet) Or the folder in which the current .app is located (usually it will be /Applications/)

see src/piqopiqo/settings_panel/
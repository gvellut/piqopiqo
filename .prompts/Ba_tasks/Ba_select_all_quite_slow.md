Shortcut Cmd A (Select All) : takes a lot of time for feedback (cells in the grid visibly selected): it is instant in Adobe Bridge with the same number of photos. In PiqoPiqo : the GUI is blocked (eg scroll) while the cmd A is being processed (in Bridge scroll can be performed immediately after the cmd A and all the cells are visibly selected at the end of the scroll). Also if applying filter after cmd-A has been performed and finished (many visibly selected) : blocks the GUI for a while : visibly quite a bit slower than when a single one is selected and apply filter.

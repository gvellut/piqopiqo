Verify the rendering of the image in fullscreen (with 100% magnification or otherwise). 
I am comparing with Adbobe Bridge and it is slightly different : the image for piqopiqo is slightly more yellowish.
Check the code for extracting the full image from the file : tell me if there are options to apply that could change the color of  the pixels (from the same image).
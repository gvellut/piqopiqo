
settings :

settings => option set by user to influence the program; settings screen : Do this now. Currently defined in src/piqopiqo/config.py (reference everywhere should be removed)
state => choices made by the user in the course of using the software (eg. names, checkboxes, path, folder). Already done : you will need to integrate the settings into this

Already done but retold :
Use Pyside6 QSettings for persistence and extraction
Use Native Format
Always use the type= argument in .value() to ensure your booleans stay booleans and your integers stay integers across app restarts.
For complex types (Dicts/Custom Objects maybe list ?): Use the JSON approach (parsed from strings / serialized to string), as QSettings doesn't handle nested Python dictionaries natively across all platforms.
Create local instances of QSettings (or the abstraction on top) to use

Use a group "Settings" for the values for the settings (different from the state).
Use lowerCamelCase for the individual keys (e.g., theme, fontSize) and UpperCamelCase for your groups (e.g., UI/, Network/).
Setup StrEnum for the different state names + for group (different enum) : like for state. Use upper case name for the enum names.

Like the state : stored in the QSettings ; using the same --dyn option (memory based settings, loaded from the default).
The settings use the same QSettings backing file as the state (create with QSettings() : the app name org .. are set when launching the application : done already)
Should add settings read and write to src/piqopiqo/settings_state.py and remove all reference to config.py in the rest of the code (keep the file so I can check everything is done)
Change the names of the store to QSettings + functions to act on it. The separation settings / state is mostly one of convention and clarity (thecnically, the are stored the same way).
Put the settings enum in a different Enum form the state enums.

Like the state : if config setting never set : they are not saved in the QSettings at application launch. Only when explicitly saved using the settings panel. Otherwise, they use the default.

It should also support overwriting settings with an env var like what is done in config.py (both lists). The env var takes priority on both the default value (if never written in QSettings or the value written in QSettings). When using the Settings panel, the value used for that will the value passed as env var, so if saved : will overwirte the original value (this is OK)

The list of settings to support is in src/piqopiqo/config.py. There are 2 list of settings : For clarity, you should create 2 enums with the 2 lists. The list in ConfigNoUserSettings is the list for which no User interface needs to be defined (see below). I refer below to that but they are an enum class. The other is the settings that are editable by the user and an interface generally needs to be defined for them (see below)
The ConfigNoUserSettings items will not be saved in the QSettings. They are set to a value at app launch : they will not be updated later. They are pure memory. You can use a different store for them. If a setting was eiditable in the past but no longer, I will move them to a different enum and even if a version for them exists in the settings : they will need to be read from memory (with the default values set at launch from the code).


Settings panel :

Contrary to the state, which is implicitly saved when the user interacts with the application, there is a settings panel that allows changing the values. 

Menu : in macos : at the usual place (probably in standard by PySide / Qt if set to the correct role). Otherwise in File > Settings... on platforms other than macos.

The panel will be  autogenerated from list of Editing elements that refer to the settings enum they edit, grouped together in Tabs. The panel refers to the options explicitly : if a setting is not referenced then it wont be editable (it can use the default loaded from launch or if the option had been editable in the past but not currently, then it will use the version in the QSettings but the user will not be able to change it).

Define a way to specify this in code + based on the user settings in config.py : try to fit them in Tabs. But should be easy for me to change
for ex : Not a spec but an idea
[   Tab("Paths", fields = [Setting(SettingsEnum.EXIFTOOL_PATH, Path(is_file=True)), Setting(SettingsEnum.CACHE_BASE_DIR, Path(is_dir=True))],
    Tab("Shortcuts", fields = [ Setting(SettingsEnum.SHORTCUTS, ShortcutEditor))]
]
=> then 2 tabs with fields in each and specific editor
New options can be added, and existing options can be removed : they can be removed as in not longer editable  or deleted from the list of config options in the enum.

Mode for saving :  
- ConfigNoUserSettings (set by the application)
- autosave (value saved immediately) without save. When tabbing to the next option or when focus exits the field (text field) or when value is changed (eg combobox) => immediate
- save button after edit one tab 
- need to configure the panel differently depending on that setting (but support both)

Each config option starts with a reference to the field in the enum (python reference so easy to follow, not a string).
The value set in the settings_state.py will be used to set the default value (before it has been changed by the user).\


Type : Folder or Path => must exist or popup when open to set those files
Choice : single or multiple : checkbox or radio or combobox ; set of names with values (enums) possible or set of names with name as the value
Text
Number
list of texts
+ way to group multiple settings in a grouping panel like HTML fieldset with legend
+ custom panel for ex : STATUS_LABELS : need to set a list with color, name, number from 1 to 9 to refer to an avasilable shortcut) + Shortcut editor (done by hand : ie user must type "ctrl+a" and not have to listen to the keypress event from the keyboard)) => write the custom panels

Depending on the mode for saving : In Save button mode add a Save and Cancel. The save will be common for all the tabs.   If detected that an option was changed by the user but Cancel is clicked : add a confirmation dialog. So need to record that.


Put all settings panel related code in a specific submodule (not in root)
save the last GPX folder in State : do not show in field. But if click browse and field empty : use that path as starting point
If field is not empty : start at the folder of the field value (ignore Last GPX folder)
Save the folder when OK from the file browser (after Browse), not when cancel
If no last GPX folder : use "" (default open location for the system)

LAST_GPX_FOLDER

src/piqopiqo/ssf/settings_state.py

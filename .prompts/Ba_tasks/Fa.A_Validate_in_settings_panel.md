Apply same validity checks when doing Save in Settings panels src/piqopiqo/settings_panel for the mandatory fields.
If not valid when doing Save (not on open the panel) : display an error and  a line beneath the field  that will show the determined default value + button : "Set to auto" (like with the custom dialog you created that shows on program open)

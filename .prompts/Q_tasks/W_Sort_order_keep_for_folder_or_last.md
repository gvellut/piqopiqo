Sort Order (View > Sort by > ...) needs to be saved in state  src/piqopiqo/settings_state.py
When set in the menu by the user : saved to state
When reopening with former folder (saved in state) or with load folder by user: set to the saved sort oder. If an image is selected, it stays selected and visible.

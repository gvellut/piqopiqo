When moving between 2 images : if one image has a keyword (on multiple lines) : the height of the multi line text is made higher. If the other image does not have keywords : the height of the field is made smaller. I would expect this would not move either the labels (on the left of the fields in the metadata panel) or the text fields above the keywords. But it does. This also happens when moving between 2 images with keyword height different (ie both filled with at least 2 lines but not the same number). There is some delay before it settles finally at their final position. When the keyword height is the same : I don't see any movement.
So check what is going on and make the layout as stable as possible. Only the keyword text field must move and only if it is different. The fields above should not move. The fields below it can be moved up or down : but only visible movement is translation to fit the height of the keyword text field. I seem to see up AND down but it is fast so hard to be 100% sure.
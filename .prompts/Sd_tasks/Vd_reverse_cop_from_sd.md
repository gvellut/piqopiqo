Copy from SD tool : add a checkbox in the screen before copy starts (with the dates) to reverse the folders : current order (standard) is <date>/<volume name>. That checkbox would copy to <volume name>/<date>. Make the state of that checkbox a state BUT depends on each volume name : ie if checked for volume rx100 => leave checked for subsequent copies (unless changed later), But if volume is xs20 : not checked. The check states of both volumes are independant.
Change the Flickr reorder album summary. Use a no edit multiline text field (data should be selectable). See the Flickr Find & Replace.

Also Edit > Find Replace : if relevant (check), make sure the summary is a edit multiline text field like the Flickr Find & Replace

Both flickr + Edit Find replace : The & of the menu item is not rendered so 2 spaces "Find  Replace" instead of "Find & Replace". Fix for both
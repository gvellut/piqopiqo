add filter shortcut in grid : ie alt + label shortcut for search (there are the defaults : can be changed by user in settings) 
shortcuts to enable  filters : , Alt ` (for No Label) Alt 1, 2 ,3, 4 .... for label with text : the effect of the shorcut is to change the presence of the label filter ( unchecked to checked and back). It will also update the checkboxes of the corresponding label in the filter toolbar (or it is synced automatically from the filter value).

note that in macos keyboard layout : the alt + char is another character. Not sure it will impact. But the Alt + ... are valid shortcuts in other software.

Add a shortcut in grid to cycle among folders : Alt 0 to set All Folders  Also :  Alt = to next folder filter (from the current one) / Alt - to previous folder filter ; those last 2 shortctuts do not pass through the All Folders filter : except it can start from there. Ie filters are : All folders / tx95 / xs20 => Originally on All Folders : Alt = : go to tx95 ; Alt - go to xs20 (not back to All Folders) ; to get to All Folders : Alt 0. If only a single folder (folder filter disabled) : no effect.

Add a shortcut in grid to clear all filtere : alt \

Cmd F in grid go to edit Search text field

The Alt shortctus are not valid when a text field is edited (metadata edit panel or Search field )

Those shortcuts have no effect in fullscreen (and probably should not even be handled)

The shorcuts can be changed by the user in the Settings panel. LAbel Filter shoprtcuts, Folder filter shortcuts, Clear filter, Search text field
Update the layout of the shortcut editor to have 2 columns : make the Shortcuts its own tab (instead of common Labesls + shortcuts)
Also add sections in the tab : one is for setting labels (ie Label 1, Label 2..., No Label) ; Then there are shortcuts for the grid (select all, keep last selected), Filter shortctuts (described here), Fullscreen shortctuts (Zoom in out reset)
Possibly since the shortcut rows are not very large : Make 2 columns of sections . So with 4 sections : they would be 2 rows : setting labels and  Filter shortctuts  (you can put the label filter shortcuts next to the Set label shortcuts) ; then grid shortcut and Fullscreen shortcuts on the second row.

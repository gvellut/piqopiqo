Add  popup panel to see the current folder, list all the folders in the current folder that are considered (coherent with the folder filter for ex ie those with a photo in them) and cache location by folder (inside the cache directory)
in File > Property

- way to clear the thumbnail cache of a folder or for all + clear Metadata (warning) for a specific folder or for all 
+ see number of photos statistics
"Go to folder" buttons for each folder
If too many folders. Put in a scroll panel.

OK Cancel

- After exiting : on OK : if cleared : the thumbnail or exif msut be read back (like when Opening a new new folder)


Also add an About Dialog ; version (from __init__.py), date, a link to Github project https://github.com/gvellut/piqopiqo 
(standard location in macos : below the name of the program ie QAction::AboutRole; standard location for other OS : in a section below the File menu  ) 
Use a QMessageBox::about

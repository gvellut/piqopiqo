
settings :

see https://github.com/pythonguis/pyqtconfig

separate db from the state DB (one for each so easy to clear by deelting)
Stored in the support dir (get_support_dir) like the state DB.
Same format : Use simple structure : Key TEXT , Value TEXT. If the value must be an array or some python structure : it must be serialized as string and parsed as JSON.


NativeFormat


add --dyn (dynamic) option => to ignore saved settings + state + do not write : for dev or testing. All in memory.
Add --settings option to cli : to choose another dir for config : will be saved or read from there

Settings panel :
Menu : in macos : at the usual place (probably in standard by PySide / Qt if set to the correct role). Otherwise in File > Settings...

panel autogenerated from a subset of the Config.py options, grouped together. These options are referred to as editable options below. Non editable options are read-only from the POV of the user.
New options can be added, and existing options can be removed : they can be removed as in not longer editable  or deleted from the list of config options in config.py. In both cases, remove from DB

config options : 
- non editable
- editable but in the QSettings (so can be set by user in some way)
- not editable (can change between version)


2 modes : 
- autosave (value saved immediately) without save
- save button after edit one group


Each config option starts with a reference to the field in Config in config.py (python reference so easy to follow).
The value set in the config will be used to set the default value (before it has been changed by the user).
After intial launch, the dbs are created and the config.py is no longer used : ONLY for the config options that are editable. Otherwise : taken from config.py
=> so when reading an option, must check if is editable, it should be in the DB, this is the value ;   if not editable, check the value in config.py
Process the --dyn option cleanly.


Type : Folder or Path => must exist or popup when open to set those files
Choice : single or multiple : checkbox or radio or combobox
Text
Number
+ groups
Apply order of  priority : Config.py < Saved config (can be there but empty : taken) < variables passed by user (can be saved to config if settings saved later) : on top of each other
settings panel : GCP project + Flickr API config + cache dir + list from config.py
Dialog settings
config application : sqllite ? or json dans support : 

Add option : still read from config => for testing : immediate
config.py => default : otherwise copied to file config if not exists 
qq configs : uniquement env var : pas dans settings
ou être explicite sur les options de config disponibles
> in 13.settings.md : rewritten ?
Need to intercept cache if folder not there
=> by default in support
Or allow user to set

Mandatory options
Need to check for exiftool => need for lowres tb + metadata extraction
Check if exiftool exists.
Test /opt/homebrew/bin/exiftool => set this in settings if OK
Settings panel opens with path to set exiftool
https://aistudio.google.com/prompts/1pFRjspBffvNCBOxSF3IqySH-oPOCx2hQ


if not there : dialog
Do both at the same time
    # Add verification for existence of cache dir : + dialog to set at startup
    # mode : dyn (no saved settings => read from here) nocheck (no dialog => crash if issue)
    # kw (for keywords saved) + state (for reading / persisting state)

    # exiftool : add verification + dialog to set at startup

Settings panel will also include a  to see the current folders and cache location by folder + way to clear the TB cache of a folder or all + reload Metadata from exif (warning)
+ see number of photos statistics
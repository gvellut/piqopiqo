In the Copy SD card dialog : at the end : there is an option to eject the card. Currently : if checked and OK is clicked, the dialog closes and there is no info on when the sd card is ejected (it is unclear if the card is ejected or what happens ; multiple times I click OK and wait for a few seconds : I remove the SD Card from the slot and get the macos message : Disk not ejected properly)
Instead : when the eject is chosen : add a screen to the dialog : screen Ejecting SD Card... with a progress bar (with no total : so on macos will be spinning) with Cancel button. When ejection has been finished : new screen to the dialog : Remove the progress bar and display the line : You can remove the SD Card safely ;  OK button : OK closes the dialog. If cancel was pressed : just close the dialog (nothing to ask to the user)
Materialized selections
Enter photo grid for the stacks
Reorder manually

Keep selection filter in stack (so can exclude visibility) : or new level of filters ?

Select one and exclude the other : need to define label for select and exclude
Or reverse selection : so can exit, reverse selection, set label
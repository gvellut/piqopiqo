When a subfolder of the currently displayed folder is moved in the file system to outside the hierarchy of the current folder, the images that were inside the subfolder are still displayed in the grid. Also when trying to go fullscreen, the fulllscreen image is blank (makes sense since the image is not there) but it it not great UX.
Use watchfiles if possible to detect when folder and images inside are no longer in the hierachy of the current folder and remove them from the grid. Remove them from selection (like when an individual image is moved or renamed; keep the others previously selected that are still in the grid but do not add).
You should also detect when a folder / an image (I think it already works if it is an image that is moved or renamed) is moved or renamed but stays inside the hiearchy : in this case it should stay in grid (OK if hte thumbnail needs to be regenerated; after all the path was changed) and the fullscreen view should work (instead now it is blank, like when the file is moved outside the hierarchy ; the former path is now invalid)
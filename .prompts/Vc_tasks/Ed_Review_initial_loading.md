Review the initial loading if Order by Time Taken
- If I wait without scrolling : after enough time : the order of the images seems fine at the end
- If I scroll while the images / EXIF are loading : evntually the order of the images is fine. BUT temporarily, they are not : even though the EXIF has loaded : they are out of order. If I stay on a location : I can see out of order photos. If I wait enough time, the reoorder seems to happen but the reordering should happen while loading (using the batch for the number of photos after which to update the ordering)

Also when I use the File > Property dialog : and click on Clear Metadata : the batch is not taken into account : instead the update takes place right asway and the thumbnails blink as they are reordered right away. 

Fix both.
Settings : Add Label : red border for invalid does not go away when writing something. Need to go away immediateley if not empty, as long as no 2 labels have the same value (invalid)

src/piqopiqo/settings_panel/status_labels_editor.py

Also in settings in general : make it so that focus a text field and edit then Enter in a text field does not Save the settings. Instead, it should unfocus the current field and focus the Save maybe (but NOT save by itself)

In Label status : if red border still shows  The Save button does not seem focused (not blue) or even enabled  but an enter in any text field will Save the settings : incorrect.

Also some incoherence : the Browse button are not blue (OK), but if tab Labels / Shortcuts : the Add Label is blue. It should not become focused when going on that tab.    
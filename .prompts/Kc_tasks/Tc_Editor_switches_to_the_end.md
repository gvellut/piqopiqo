When switching to another app then come back : editor position in text field in metadata edit panel (eg title) is moved to the end : text field. Only if the text field was edited before switching : when not edited and switching / then back : same position. It seems something weird happens when autosave the field. What happens when saving ? why is that happening ? It is a bug. Fix it
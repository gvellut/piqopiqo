Add dialog : make it smaller : align the Label Name URL to the left . With the text field taking the rest of the dialog width (text fields must be aligned on the left with some space betwen the label and the field)
DO NOT USE a Predefined... label for the Predefined choices !!!! Just add the combobox aligned with the text fields on the left : do not take the whole width , just as large as needed for hte options/ Also when a Predefined is chosen , also replace the name with its name (on top of the URL). In the combo box : do not have Predefined as one of the choices

When returning from the add dialog OK : do not make the new entry selected in the user defined map list (it is blue). Nothing must be selected.


Change the Edit button to a icon with a page with a pen.

In the user defined map list : make the + button in green (the + is green : the + is made of 2 rectangles with width, not just lines like now and not a character). The - button should be red (also some wdith, not just lines ; and not a character). Center the + and - veertically in the button. Same thing for the edit : They are higher than should be. There is some additional space inside the button at the bottom.

Also Opening the list with Edit on the Interface tab : I did not click on the list : There was no item active (blue background) . if I click remove, the first item is removed : the remove should not have been active then. Unless one of the entries is active (blue background) : the - button  must be inactive. Same thing with the edit button.

In the medata edit panel : When the Map button is present : the longitude field must be the same wdith as the latitude field (aligned on the left AND right). Also there is some additional space between latitude and longitude lines, 
In the Medata edit panel, if enabled (see below) : add a "Map" button on the first line (of the 2: latitude and longitude) to open a page with webbrowser.open_new_tab (page indicated as a template with lat and lon place holder, defined in the settings panel; see below)  If there are multiple options defined in the settings : the button will unroll below it the list of options and a click on one of those will open the page. If there is only one : a simple click will do the action. The button is inactive if there is no lat lon defined for the photo. If multiple photos are selected, the fields are <Multiple Values> : the button is also inactive. You can check for that either on the string that is invalid for a lat lon or identical to <Multiple Values> or if multiple are selected : inactive. Whatever condition is simple. if you use the constant MULTIPLE_VALUES

Define a user setting in Interface tab. It will be part of the group Metadata Panel (existing already). It will have a text label summary : x options defined (the number of options defined). If there are 0 option defined : the button Map will not appear in the Medata edit panel (like now). Next to the label, there will be a button that opens a model dialog : 
in there you can define the list of options. 

There will be a list of user defined map options (displays the Name field) in the dialog and 3 buttons on the side see "Screenshot 2026-04-19 at 21.34.17.png" (except there wont be the first button : Clone , just the 3 below): Add Edit Delete. Use basic icons prefedined, use tooltip. 

Button OK Cancel. Do not make OK or cancel focused. When OK : summary label in the setting panel should be updated. The value should be serialized to a JSON and saved in the QSetting (like the other complex settings in the panel like the config for status labels)

When Add is clicked a dialog opens with 2 fields : 1 labelled Name (which will appear in the Map button in metadatpanel if there is an unroll), the other one labelled URL : it will be a standard URL with placeholder for lat lon with the {} syntax eg {lat}. Just validate the only placeholders contains both {lon} and {lat}. Below the URL field : there is a combobox Prefefined... When cliked it unrolls : with entry Google Maps and OepnStreetMap (make that list easy to modify in the code ie in a constant). A click on one of the options fills the URL (if already data in the URL field simply overwrite , no confirmation). For Google Maps : https://www.google.com/maps/search/?api=1&query={lat},{lon}. For OSM : https://www.openstreetmap.org/#map=15/{lat}/{lon}.  Buttons Cancel OK. OK is inactive if the name and URL are not filled (non empty characaters ; with URL valid for the placeholders ; also the field is marked standard Qt is invalid or empty). Do not make Cancel focused. Instead Name should be active so can enter right away. Tab works.

When remove is clicked : remove the selected from the list of options. If none is selected, the button is inactive. No validation : jsut remove

When edit is clicked : the same dialog as add is opened. Do not make OK or cancel focused.
In the grid, add indicator of last selected (for the COLLAPSE_TO_LAST_SELECTED or arrow ; set when selecting or multiple or set when returning from fullscreen) : add a dark blue border above the seletion indicator (light blue)

Also when doing Select All with the shortcut : Make the first item in the filtered in items the last selected (instead of the last it seems)

Also when TOGGLE_RIGHT_SIDEBAR is activated : since the number of rows and columns stays identical : do not change what items are visible (instead of scrolling to the last selected)
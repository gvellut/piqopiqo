Flickr upload : Use a boolean instead of those strings

UPLOAD_SCOPE_VISIBLE = "visible"
UPLOAD_SCOPE_LABEL = "label"

Put the checkbox for the upload scope at the top of the dialog

Remove the space between the number of photos and the warning on Keyword / title missing

Put the album name / id in a labelled panel

Remove the label for Token + path : if not there the button will change to somthing for login
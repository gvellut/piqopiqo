Currently FLICKR_UPLOAD_MAX_WORKERS is for all the steps or the flickr upload.
I wnat 2 RuntimeSettings : 1 for the upload itself (ie images sent to Flickr)
another one for the other operations (reset query, etc )=> by default this will be 1
The FlickrUploadManager must have both
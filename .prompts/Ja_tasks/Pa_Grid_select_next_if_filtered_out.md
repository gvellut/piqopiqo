photo Grid : 
when setting label using shortcut and as result the selected photo is removed from the visible photos because of filter : select the next photo that is filtered in (next : in the order of the grid). If no photo after (last) : select the one before (so the last after filter). Or none if no photo.
If multiple selected and the label is set with shortcut so the selected photos are no longer filtered in : use the last selected (same idea as which photo serves as the base for the arrows if multiple are selected) as the base : Select the photo (still filtered in) after it or if not possible the one before (in the order of the grid)
If the photo or photos is still in the filter after the label change : they stay selected (no change from now).
Check if there is already something for that in the code / for single or multiple. If not do the work

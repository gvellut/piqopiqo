Add a shortcut to the photo grid :  Esc
Add to the User Settings for setting in the Panel ; src/piqopiqo/ssf/settings_state.py

In the grid (not fullscreen) when Esc is pressed : consider the last selected (same idea as which photo serves as the base for the arrows if multiple are selected or return from fullscreen), remove all other selected images from the selection set. If a single item is selected, no effect.

see commit 86e3c9e0eb57459da86724ec405dced6f745c994
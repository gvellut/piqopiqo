Add User Settings (True by default)  : Protect non text field from edition : boolean : PROTECT_NON_TEXT_METADATA
in Settings Panel located : in Tab Interface ; group Metadata Panel below Show description
If true all the fields except : Title, description and Keyword are read only in the text fields of the GUI only (it does not extend to the metadata DB ie tools can write to it). The fields will still be refreshed if needed => apart from the GUI read only / blocked edition : they behave the same as now.
Make the list of fields to be made editable easy to edit : not in the middle of code.

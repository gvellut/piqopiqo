could you check where it comes from : and what could be done to fix : some small but noticeable delay : both when filtering in a label (checking the box in the filter panel) and no filter (no label checked) ie all the photos (weirdly) => both a little delay before the grid is updated ~ 1500 images. what operations are performed when these 2 actions are performed ? how to optimize ? 
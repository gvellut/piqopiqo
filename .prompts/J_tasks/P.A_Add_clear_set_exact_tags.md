- Add a Clear lens info to the Set Lens info combox : this will clear all the lens info in the Metadata DB for the relevant images. In the confirmation dialog : insidcate the tags that will be cleared : (do not use '' as the value : indicate they will be cleared and list the tags)
- I gave you the tag names : `-lensmake=Samyang -lensmodel="Samyang 12mm f/2.0 NCS CS"  -focallength=12 -lens="12.0 mm"  -LensInfo="12mm f/?" -FocalLengthIn35mmFormat=18`. Use those tag names with the case : in the confirmation dialog and in the write EXIF (for Save EXIF and Flickr upload). Do not use prefix.

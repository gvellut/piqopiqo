Copy SD tool : BASE_EXTERNAL_FOLDER is not configured : add a button to open Settings panel + open the external / Workflow tab +  focus actual control.

Review all the checks in the code for the presence of a configuration option. If its absence results in a dialog saying option ... is missing or such. add a button to open the correct tab in setting.

The same way as when the missing Flickr API key or secret is missing in launch_flickr_upload src/piqopiqo/tools/flickr_upload/dialogs.py
Remember the time shift for previous gpx2exif runs src/piqopiqo/gpx2exif depending on photo folder name + last in all the folder names + limit to memory for time shift for folder names (last 10 folders, evicted in LRU) + apply the remembered time shift if not already set in Folder Data to dialog from src/piqopiqo/main_window.py _on_gps_time_shift and _on_apply_gpx

- Add the number of folders to remember to Runtime settings src/piqopiqo/settings_state.py : TIMESHIFT_CACHE_NUM
- Add LAST_TIMESHIFT_BY_FOLDERS State data src/piqopiqo/settings_state.py : as a dictionary from subfolder name to time shift string. eg : if the root folder (the folder that is currently open in the application) is /Volumes/MySSD/photos/20250506_annecy and photos are in  /Volumes/MySSD/photos/20250506_annecy/tx95 and  /Volumes/MySSD/photos/20250506_annecy/rx100 => 2 folders tx95 and rx100 that will be the keys to the dict. See src/piqopiqo/main_window.py _on_gps_time_shift _on_apply_gpx for the folders. That dictionary must be ordered (default) to implement the LRU 
- Add also a state for the very last timeshift independent of folders : LAST_TIMESHIFT
- That state data is saved when timeshift determined from photos : ExtractGpsTimeShiftConfirmDialog in src/piqopiqo/gpx2exif/dialogs.py. After the timeshift has beed determined and saved to Folder data : saved to State for the folder (LAST_TIMESHIFT_BY_FOLDERS) and very last (LAST_TIMESHIFT)
- Also saved when  src/piqopiqo/main_window.py  _on_gps_time_shift _on_apply_gpx after OK by user and before actual launch of the data update (_on_apply_gpx) (if the timeshift was set through ExtractGpsTimeShiftConfirmDialog : it will be the same but it is fine ; it might refresh the order of the LRU cache ; fine)
- The values are recalled if the Folder data for the timeshift for a folder is empty and  src/piqopiqo/main_window.py _on_gps_time_shift _on_apply_gpx  is called. The folder data takes precedence if it exists takes precedence. If not there but there is a LAST_TIMESHIFT_BY_FOLDERS for the folder name : take it. Finally, if there is a LAST_TIMESHIFT : take it (unless the TIME_SHIFT_UNKNOWN_FOLDER_IGNORE is set). Otherwise it is empty or "NOT SET" (_on_apply_gpx) : but see below for refactoring of that part.
- in both those dialogs : if there is data and it does not come from the Folder data but from the State : add a Red label (smaller font) : "Previous"

- Also add a User Settings option src/piqopiqo/settings_state.py : TIME_SHIFT_UNKNOWN_FOLDER_IGNORE : in settings panel :  src/piqopiqo/settings_panel/schema.py : in GroupSpec title="GPX", "Ingore last timeshift if unknown folder" => so if folder not found : the LAST_TIMESHIFT is not used. Only LAST_TIMESHIFT_BY_FOLDERS is used

- Refactor the menus Apply GPX... and GPS Time Shift... : remove the GPS Time Shift.. menu and _on_gps_time_shift : Instead put the text fields of the dialog there inside the  Apply GPX... dialog : so this dialog takes the functionality of GPS Time Shift... dialog. The tiimeshifts need to be valid for OK to be enabled. When pressing OK : the timeshifts are saved in the Folder Data (+ in the LAST_TIMESHIFT_BY_FOLDERS and LAST_TIMESHIFT states).

In upload to Flickr tool: when there are photos with missing tite or keywords, Cancel selects them. If they cannot be selected with the current filter, the filter is cleared. Add a small update: If the photos missing are for when Use Lifecycle is checked (so the Missing wearning is displayed  when Use lifecycle is checked and cancel is pressed), also add the upload lifecycle label (defined in the settings) to the filters (so clear all filters and make the lifecycle label active then select the images with missing metadata). If Use Lifecycle is not checked (and they are missing metadata), only clear all filters + select the imaes with missing metadata (like now).
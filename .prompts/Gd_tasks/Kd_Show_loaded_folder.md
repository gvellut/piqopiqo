display current loaded folder in the status bar : use same scheme as for the favorites in menu (if Favorite Folder defined, relative to the Favorite Folder : only the part relative to it), otherwise if too long (add as a Runtime Setting : the proportion of the status bar taken), but existing controls take precedence. add ellipsis (use src/piqopiqo/components/ellided_label.py ?). In the center of the status var : make sure it never moves if the bottom left indicator of number of photos + selected changes or if other controls appear on the right or left of that label.
If no folder opened : display No folder loaded (add a constant with the string)

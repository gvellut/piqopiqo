In Flickr Upload : when the upload is blocked because some photos are missing the mandatory fields (Reject upload if title or keywords missing), When clicking Cancel : select in the grid the photos which are missing those fields. Make the first of those photo (in the order of the grid) the relevant photo (for Esc COLLAPSE_TO_LAST_SELECTED or arrows ; the one with the supplementary highlight ) If some photos are not visible (If Use Lifecycle is used) : clear the filter first (only if some are not visible : if the photos with missing fields are all visible : do not modify the filter). Also scroll so the first (which has the supplementary highlight ; already something special for that one in the code i think) photo is visible in the grid.
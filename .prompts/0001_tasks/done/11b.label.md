In the grid view, the date of the time taken data is shown. Correct the formatting : it should be ISO : ie 2025-10-11 13:13:30 not 2025:10:11 ...(change the separator for the dates)
In full screen : in the date : instead of the date of the file in the file system : also use the time taken of the Metadata DB. If empty, use the File system date (like Now) but color the text in red



Also in fullscreen mode : if there is a color icon : the height of the overlay text changes. It is visible when moving from a photo with a label and one without, It should stay the same height. But you should change the layout anyway : Change the layout of the overlay : make it vertical. 
start with the color icon. If there is no label for the image, Keep that space empty (ie nothing displayed above the black background) but the space is still there
Then below : the name of the file (always full, never ellipsed).
Below, the time taken (from DB) always in the same format : Make sure the date. If not time taken in DB, same as now
The bottom of the vertical layout should stay the same space above the bottom of the screen (never moves) + add that value in config. Add also in config the sapce from the side. Add in config also configuration of the position : top or bottom (the default ). Always on the left side. if at the top, height in config applies to the space between the top and the layout.
Add 2 things :
- Edit panel src/piqopiqo/panels/edit_panel.py : when user leaves field to focus on some other GUI elements => Save in DB automatically (like with Tab or Enter) so never data loss
- exif fields EXIF_FIELDS in config.py : instead of simple list of strings : add attrs @define class : add label (possibly None : default) in model => show in UI src/piqopiqo/panels/exif_panel.py isntead of the string used to extract from exiftool; If no label : the exiftool key is used (except if auto format : see below)
    - Add string names for the existing ones
    - config.py : option auto format for display (used if no label) => Value is True : then if true, if no label : remove the prefixes + format around the capitalized letters : for display File:FileName => File Name EXIF:DateTimeOriginal => Date Time Original
        - add function for this in the exif

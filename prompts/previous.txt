
- another panel on the right will include some data of the selected image in the file system like filename, date created, date modified (that list of fields to show must be set in the configuration : so can be only filename in it)
    - the panel will show the field names + a field value (empty if field not there or empty). The value must be formatted suitably (for example date in YYYY/MM/DD HH:MM:SS but this should be sewt in the hardcoded configuration)
    - the list of fields will be hardcoded : technical way to retrieve it from exiftool + 
    there will be other fields for the images later (from EXIF, XMP, ITPC) so make sure to indicate the origin of the data (filesystem) in the configuration structure
        - the origin of the metadata (exif, IPTC, geo, XMP, File system) must be clear (for example use a prefix ie "fs:..." or have multiple parts for the origin of the field) in the list of fields to display
        - the label of the field will be 
    - In the first version, there will be no edition.
    - the exif will be retrieved from the data file on click (not stored for now)
    - the list of fields to show will be listed in the configuration


- The 2 panel widths can be resized with dragging the border (indicate the name for that GUI pattern)\
- the layout (panel widths), window position / size must be saved and reestablished on open
- There is menubar with the basic macos setup.
- The language of the menu and logs will be English only.
- Use standard Python logging for logging info / debug / warning to console


- sort automatically by EXIF date created or file name (in the file system)
    - possibly sort manually by drag and dropping a photos
- The GUI will offer a panel to see and modified EXIF tags of the image: If an image is selected with the mouse, or the arrow keys are used to select an image, the data shown will apply to that image:
    - see the EXIF data : base EXIF, IPTC, Geo, XMP, more fields
        - a subset is sufficient : the ones that are included after shooting with a digital photo camera
    - a way to edit the EXIF data in 2 ways :
        - first : a way to store them OUTSIDE the image, ie a SQLlite database (for example) that will refer to images in a certain folder and include either all the EXIF data (exported, possibly modified), or just the data that was modified by the user => the full data wil be a merge of the EXIF data in the image AND the data in the external database
        - second : a way to write that external metadata inside the image files => this is not a core requirements. However, I need to know if libraries are available for that
        - this is different from Adobe Bridge which writes the EXIF directly into the files (no outside database).
    - multiple images can be selected in 
- the program will have a way to view an image as full screen by pressing a shortcut (space by default)

 title, description, label, other EXIF metadata 
 - another panel will include EXIF tags of the image: If an image is selected with the mouse or with the arrow keys : that panel will show them :
    - for now, only some of the base EXIF, IPTC, Geo amd XMP tags will need to be shown. Define in the spec a small subset (but include "Date Time Original", title, description, keyword, camera model, lens model, focal, aperture, shutter speed)
        - the list of fields to show will be listed in the configuration
        - that list will be a subset of all possible tags handled by the program (that list will be configured inside the program)
        - it will also show some data of the image in the file system like filename, date create, date modified (that list of fields must also be listed in the configuration)
    - the panel will show the field names + a field value (empty if field not there or empty) separated into sections 

    - can you get the field names + technical field names (for access through EXIF library) from a library : or must the names be defined separately : if separately, there must be a structure with all the known posibble fields (with the configuration a subset of references to them)



My goal is to write a replacement for Adobe Bridge https://www.adobe.com/products/bridge.html to use inside macOS only. But it will only have a subset of functionalities :
- the GUI and main program will be in Python

I would like for the Main panel to display a grid of square / rectangles. Currently, it will not be a grid of photos (but it will be later) but will draw something (a number) in the space for the grid (I will specify the shape of the data below).

The program will have a number of items : this together with the number of columns, will determine the number of rows in the grid. Initially, the number of items will be hardcoded to 100 with a constant in the code. The list of items must be maintained in memory by the pprgroam. Each item has 2 fields : one is a number corresponding to the position (starting with 1) The other is a text : "Item # {number}". 

Scrolling will be done row by row : it will not be a number of pixels: An entire row must scroll up or down to take the position of the row that was above, or below it, depending on the direction of the scroll.  The scroll must stop when no more item can be shown in the up or down position so the max number of scroll can be computed depending on the number of items, the set number of columns and the computed number of rows. All the grid items will be displayed entirely ie a square of the grid will not be cut off either horizontally or vertically.

The swift app will request the items to display by computing (zero-based) each item depending on the displayed starting row (depending on the scroll position), the number of columns and the computed number of rows. That request must be done async since later the item will be paths to image thumbnais, that may not have been generated when requested. However, for now, the items is maintained in memory by the program and will be returned immediately. When scrolling, the Swift code must unload the view and rerequest the zero-based index of the items it needs to display unless the items was already displayed : for example : 4 rows to display at once => scroll down by one : the top row disappears and must be unloaded (lazy grid) and the items for the new row must have their views loaded with the data requested. The others are not cancelled or reloaded.

The grid must be separated by white lines (unless there is no items in the space : only space that contain an item must be surrounded by white lines).

The number of columns in the grid will be set by requesting a number : it will be read from a config structure maintained in memory for now by the program (not serialized ; intialized from default). The structure will be created at startup. The value of the parameter will be 5 when starting the application (also include a way to set that value from the Swift Code ; used below). The width of the panel must be divided equally between the available space. There will be no horizontal scrolling. All the grid items will be displayed entirely ie a square of the grid will not be cut off either horizontally or vertically. In the vertical direction, each item must start with the same height as the width of the items. But if they would be split because of insufficient space, the split row is removed and the height of the other rows will be increased so all the vertical space in the panel is taken by the items so that in the end , each item will be a rectangle, with bigger height than width. If there is not enough items : for example, if the computed number of rows according to the available space is 4 : but with 5 in the number of columns, there should be 20 items displayed at once. But there are only 12 items, 8 spaces in the grid must be left empty : those spaces at the bottom of the grid will contain nothing. Nothing will be displayed, the space if there were items will be left empty but space of remaining items will not be increased.

That number of colums and rows will not be recomputed if the panel is resized (for example if the window is made smaller by the user) after startup, **unless the number of columns is changed**. Instead, the view inside each square of the grid will be made smaller or bigger when the window is resized. The view must provide a menu : Increase number of columns and Decrease number of columns. And the horizontal space for each item must be recomputed and the vertical hieght for each item must be recomputed by the process above. Which item to display must be determined. The view will adjust the scroll value by trying to keep by prioritizing the first diplayed items. Ie if it determines there is only one item displayed, the item in row, column 0,0 will be shown. Depending on the number of columns and rows, that item may be moved in non-0 columns but will remain in row 0. Only the newly displayed items must have their data requested. The ones that were already there before the change in value must not be requested again.

Each item will be draw with as follows : The text will be at the bottom of each square and will take the space of one line of text : if it should be cut off, use ellipsis. Then for the remaining space, draw a reectangle with gray background : with a margin on all sides hardcoded in the  code but set to 10px initially. Draw the number in some font larger than the text field (but you do not have to adjust that font size to fill the space)

As a first step : explicit the algorithm with pseudo code, depending on the chosen number of columns, the availalbe width and height in pixels. What happens if the chosen number of columns is changed. Explicit the variables. If you have some issues or if it is unclear, think and ask questions.

NO CODE except the algorithm in pseudo code. 



Correct the following :
- I mentioned : 

That number of colums and rows will not be recomputed if the panel is resized (for example if the window is made smaller by the user) after startup, **unless the number of columns is changed**. Instead, the view inside each square of the grid will be made smaller or bigger when the window is resized. The Swift view must provide a menu : Increase number of columns and Decrease number of columns. When one of those is called, the Rust config that maintains that value must be changed by calling the Rust Core lib. And the horizontal space for each item must be recomputed and the vertical hieght for each item must be recomputed by the process above. Which item to display must be determined and the items must be requested by the Swift application to the Rust Core Lib. The view will adjust the scroll value by trying to keep by prioritizing the first diplayed items. Ie if it determines there is only one item displayed, the item in row, column 0,0 will be shown. Depending on the number of columns and rows, that item may be moved in non-0 columns but will remain in row 0. Only the newly displayed items must have their data requested. The ones that were already there before the change in value must not be requested again.

However, I see testing that the number of columns can change when making hte window smaller or bigger or when I move the sidebar (detail panel)
Also the "increase columns" and "decrease columns" sometimes do not add a column.

- there is free space that should be taken by the grid on the right of the grid (left of the detail panel),

- the last row is cut ie the bottom of the box is cut. When scrolling down, less of the bottom is cut. However, the top of the first row becomes cut (when looking at the first items on open, it was not cut). When scrolling down to the last of the list (100 items), the bottom row is not cut : the bottom border + margin is shown. The bottom (border) of the box should always be displayed as well as the margin. Same thing for the top : a complete box with the border and margin should be displayed for all rows.

- the last row is not complete : there are fewer items on that row than on the other rows. Change the list to 102 items (instead of 100) : with 5 items by row (default), there should be 101 lines, with ONLY the last line incomplete with only 2 items (this should be recalcuated when the number of rows is increased)

About this : I mentioned :

The number of columns in the grid will be set by requesting a number to the Rust Core lib : it will be read from a config structure maintained in memory for now by the Rust core lib. The structure will be created at startup. The value of the parameter will be 5 when starting the application (also include a way to set that value from the Swift Code ; used below). The width of the panel must be divided equally between the available space. There will be no horizontal scrolling. All the grid items will be displayed entirely ie a square of the grid will not be cut off either horizontally or vertically. In the vertical direction, each item must start with the same height as the width of the items. But if they would be split because of insufficient space, the split row is removed and the height of the other rows will be increased so all the vertical space in the panel is taken by the items so that in the end , each item will be a rectangle, with bigger height than width. If there is not enough items : for example, if the computed number of rows according to the available space is 4 : but with 5 in the number of columns, there should be 20 items displayed at once. But there are only 12 items, 8 spaces in the grid must be left empty : those spaces at the bottom of the grid will contain nothing. Nothing will be displayed, the space if there were items will be left empty but space of remaining items will not be increased.

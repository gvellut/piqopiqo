- filter panel in src/piqopiqo/panels/filter_panel.py
- modify folder filter : add "All folders" (default) in the combobox. remove the checkbox : the content of the combobox is the filter : so if "All folders" : no filter ; if a folder selected : filter the photos that belong to that folder. The filter is applied on selection. If only a single folder, the combobox is inactive (although it is filled like when multiple folders ; the value is set to All folders).
- filter by label (multiple) : Add all the labels configured in Config STATUS_LABELS : a checkbox, then the color swatch, then the text of the label (in default color ; ie not colored). Also add a checkbox (before the other ) : No Label (with no swatch) => The list of photos will be filtered by a OR : ie show the photos with either of the checked labels or no label (it that is checked). (AND does not make sense for the labels since they are mutually exclusive)
- search field : in keywords + title of the photo metadata. Add "Search ..." as a placeholder (to be cleared when focusing) (and if field is empty : displayed if unfocusing => probably Qt standard). When unfocusing or pressing Enter and has text => apply the filter. The filter is not concerned with words : it is purely string contains. The filter is case insensitive.
- Taking into account multiple of the filters above : it is a AND : that is, a photo must satisfy all filters 
- Add on the left (first position) a button : "Clear filter" : this resets all the filters to their default state
- add Qt objectName to all the filters
- If no folder open (see src/piqopiqo/__main__.py) : the whole panel is inactive and it is not possible to change the filter state.
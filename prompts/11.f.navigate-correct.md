- when _navigate_to_preserve_zoom in src/piqopiqo/fullscreen_overlay.py : it is the center of the image that should preserve its position, that is the center of the new image and the center of the old image should be the same in screen coordinates (even extended since the center can be outside the screen so coordinate can be negative). With the rest drawn relative to that center (using the same zoom factor). If moving back and forth between 2 images of different sizes : the view of _each_ image should remain the same. It is not currently the case. If the images have the same size, the image should not move when going back and forth (it is currently the case)


- the current implementation for _navigate_to_preserve_zoom inside src/piqopiqo/fullscreen_overlay.py is incorrect. Moving back and forth between images can result in difference the first time the loop is done : image 1 => image 2 => image 1 : the position of image 1 can be different. this does not seem to happen when the screen is filled with image pixels. Find out if this is due to the _clamp_pan inside _position_image_center_at_screen : it calls _get_effective_empty_space . The PAN_EMPTY_SPACE is 300. Change how the space is set and processed for pan : it will be in screen space (so not dependent on zoom factor). But when loading if coming from a bigger image : in that bigger image the space could have been PAN_EMPTY_SPACE but in the smaller image since the center is preserved : the space would be bigger that 300. So change: As long as that image is loaded : on the sides (either legf, right, bottom, top) where it was bigger than PAN_EMPTY_SPACE when loaded (since center is preserved ) => the space can be go up to the same space as when it was loaded even if bigger than PAN_EMPTY_SPACE (otherwise it is maximum PAN_EMPTY_SPACE). If by panning, the space is reduced below PAN_EMPTY_SPACE ie 300 : when moving to another photo : the space becomes again limited to PAN_EMPTY_SPACE (max). So it will depend on what it was at loading time : If moving to a new image the space will not be reduced to fit PAN_EMPTY_SPACE. So moving back to the first image : the center must be preserved.
- add tests with pytest : to test various scenarios of the new and old photo sizes + space.

- put in common _load_image_only and _load_current_image : they are mostly in common. Se the small image flag in _load_image_only as well like with _load_current_image.

- Also change the navigate to  new photo if no part of the new photo would be visible if the center is preserved : for exemple if the previous photos was large and the user panned : it is possible for the new photo to be completely offscreen if the center and scale factor are preserved : test if this the case if you were to apply the default process : in this case : show the new photo in base view. It means that if going back to the old photo : it will be in base view (as is currenly the case when navigating between photos in base view): but this is fine.

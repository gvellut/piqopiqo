After your last edit : see git commit number 524130edf08be7bd7d723aa5673ed6905af97c77 see requested prompts/12.queue.md

- Weird scolling artefact : when scrolling down : Only the last row of the grid has thumbnail images : but others are empty. But if scroll up : they show up.
- If select (with mouse) and only the last row visible : the rest also shows up
- When first open : all the rows are filled with thumbnails. When scroll down starts, only the last row remains

see src/piqopiqo/grid/photo_grid.py

Optimize memory in the grid :
- thumnbails : remove thumbnails (processed from the full pixels of images) as scroll up or down (with some buffer before or after the visible items in the grid) ie free memory.
- Always keep the very low res (ie the JPEG embedded inside the EXIF of an image) in memory => But if visible or with buffer : put the thumbnails above
- You can load the thumbnails (processed from the full pixels of images) after a delay : add a constant config.py in ms. For example : if stops X ms after scroll => the thumbnails should be shown but if only fast scrolling, keep the lores thumbnails

- different folders for embedded and hq thumbnails in thumb cache folder (for ex : "thumb/embedded", "thumb/hq") : keep the original name of the JPEG (instead of a suffix like with P1423600_embedded.jpg or P1423600_hq.jpg) : since in different folder, no conflict

- Make scrolling in the grid "elastic" (Scroll Acceleration / Adaptive Scroll Speed) : If moving the wheel slowly : scroll like now. But if wheel goes faster or longer, accelarate (should scroll faster). Probably standard behaviour in other GUI programs




- Add 2 parameters for the number of lines to show before and after the currently visible items for loading the thumbnails. All the items in thos3 lines will have their HQ thumbnails loaded like the visible items. And those outside it will have their thumbnails (HQ) unloaded. Set them to 1 before, 2 after. => OK : see implementation

- 
- Add a param + ENV VAR to set initial resolution of the window. Process that parameter when creating the winddow. It is usefull for agent to setup the window size so screnshot grabs are not too big. Document in CLAUDE.md.

- Correct handling of <Multiple Values> in Edit Panel : it should be treated as a special value / state (as in : this state appears when multiple photos are selected and they have a different value for the field : this string <Multiple Values> is the visible string for the state of the field) not a normal string to be edited. If the field is in such as state, simply focusing on the field (with tab or the mouse), should delete the <Multiple Values> value and the field should become empty, to be edited. Ctrl-z or cmd-z (possibly standard Qt functionality) should restore the <Multiple Values> (original state). If a field with the original state <Multiple Values> is saved with enter or navigate away with Tab : the field for all the photo should NOT be set to <Multiple Value>. It should be assumed : no edit was performed and the field for the multiple photos should not be set or changed. But it it is saved while the text is not <Multiple Values> : it should be saved (as long as value is valid for the field).

- When <Multiple Values> is the state for the Time taken : the border of the field becomes red when the <Multiple Values> is shiwb (I suppose : invalid value). It should not be red. But if the field is edited (so first the <ultiple Values> is cleared like described above) and the content set : the validation should run and the border set to red if needed. But not in the Longitude and Latitude fields : when <Multiple Values> is shown : the border is not red. Only when it is changed does the border becomes red (because not a number) : the time taken should behave like that.

- Remove the field Label from the Edit Panel. Instead a shortcut will be used (see below)

- labels : update STATUS_LABELS in config.py. Add a class with attrs @define (attrs lib already installed) for the fields. Also add a field "index".  Update the values defined in STATUS_LABELS : Remove the "No Label" value add a number from 1 to 9 for index (starting with 1). This will be used for setting (indirectly) a shortcut key for the label.

- Add application wide shortcut : for grid mode : 
- keyboard shortcuts : 
    - zoom (in full screen mode) :  "=" to zoom in,  "-" to zoom out. Same effect as scroll wheel. Reset zoom : "0" (to the base fullscreen view). In grid mode, those have no effect
    - labels (in grid mode and full screen mode) : from "1" to "9" : they set the label for a photo. The number matches one of the STATUS_LABELS values. Also set "`" : as no Label : contrary to now : this is not a String for the field label. In fact : when the no label is set for a photo : the editable field label should become NULL.
        - Also make sure the label assigned to a photo or multiple with a shortcut is reflected right away in the color icon shown on the photo in the Grid View. I should not have to interact with the grid (beyond the shortcut) to see it change.
        - If a photo has no label (originally or set in the application) : its label value should be null and there is no color icon (it is not grey or empty with borders : it is not there) above the photos

- define contansts for the various shortcuts : set their values in the config: possibly with a dict ie shortcut name => Key used. Do not hardcode the shortcut mentioned above in the code (except in the config). Then in the initialization for the grid panel or the fullscreen vioew : process the defined shorcuts so they result in the action described. In the processing : process the modifiers key (not used in what I mentioned above but it could if I were to edit the shortcuts in the config): alt ctrl and cmd (any combination) eg ctrl+r or cmd+alt+t ; seprated with + => but no sequence of keys. They must be all pressed at the same time for the shortcut to fire.
- for the labels define the constants as LABEL_1 to LABEL_9 (not LABEL_APPROVED or something to do with the string value in STATUS_LABELS)

Split the code. Use sub modules. 
Put main Qt components (along with related components) into same module. List the main components you will deal with
Split components used from  multiple main components into their own files
Refactor pan_logic : that file should not exist !!!! You should instead refactor the components so they are easily testable.
Split photo_grid. Separate the main window from the photo grid (a specific component). Those should sbe in differen files

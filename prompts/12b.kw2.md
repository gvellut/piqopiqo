When saving a keyword after Add keyword 
- correct : when entering a value in the field that opens : If pressins Enter : the Kewyword tree dialog is closed. As if OK was clicked. The keyword is saved (shown when reopening) but the dialog is closed. The dialog MUST not close on Enter for a new field.
- add : if unfocusing the field without entering anything or if Enter is pressed while the field is empty : the field is dismissed and no new keyword is not created. If unfocusing and something is entered : the new keyword is created. Even if no Enter.

- When deleting a keyword : the collapse state of the tree is reset : It must not be reset. The expanded items must still be shown. The view of the tree must be kept at the same position.
- When deleting a keyword : the dialog mentions all its children (Delete keyword "hiking" and all its children?) for single items (no children) : only mention children for the items with actual children

- the keyword state (of what are the collapsed or expanded items) mut be saved between openings : so always opened identically in the same session BUT it must not be saved to the keyword JSON


- use a QStackedWidget (or a QStackedLayout) so the tree takes the place of the Metadata edit panel. Create a new component for the tree itself + for the new stacked Widget : keep the current Edit panel component (put it inside the stack)



- Next to Import button in src/piqopiqo/panels/keyword_tree_dialog.py : add Export button : export in same format as the import (Do not export the Keywords root, just its children; do not export the Additional keywords and its childre)
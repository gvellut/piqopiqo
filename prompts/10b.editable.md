- Save editable data field on focus out (if content as changed)
- For the Label : in the combobx list : do not use colors : use standard text. Use the color as a square swatch on the left side of the name
- change the field name of the db datetime_original to time_taken (and change variable names if names refer to it).
- define constants for all the DB field names : replace the use of all strings to that.

- create db of editable data of photos when loading the folder : do not wait that a photo is selected to : read the EXIF for the corresponding data and fill the values if present
- use a backlground queue : separate from the thumbnail loading
- exif db filled in the background
- the rest of the EXIF data does not need to be in the db (can be loaded when selected).
- If a file is selected, read its data from the DB : do not read the EXIF for the editable fields. Read the exif for the other EXIF mentioned in the config.py
- if not found in the db (maybe not yet loaded) :  show empty editable fields
- when displaying the value : read from the db instead of reading from the exif from the file
- add a button Refresh with circle arrow icon to refresh the Data panel (only active if no data had been found from )

- Add a status bar : it will show the status of the loading of the EXIF and of the thumnbails with a loading bar : a single one. The thuumbnail and the editable data for each photo loading count equally as one item for the bar. When 100% : all the thumbnails have been loaded + all the data or erros for some (no retry): hide the loading bar. If there were errors : add a button with a Warning sign : with a there were errors + clickable : show panel with files in errors (statuis for either thumbnails or exif reading). Display on the left : the number of photos currently shown : if the filter is used : only display the number of photos with the filter taken into account

- Add in the config a way to show some fields of the DB : for exampe " title", "time_taken", "label" : display those fields in each grid item (photo)
- For the label : display the colored swatch on the top right of the grid item
- in the grid : keep the filename but remove the time (that comes from the File system). Instead add the fields (apart from the swatch) vertically in order below the name. The items are clamped to the bottom of the grid. All the fields are a single line. If an item is too large for the size of the grid item : display ellipsis when cutting (there is already a component EllidedLabel in components.py).
- compute the size of the footer for each grid item depending on the number of fields (font size + number). Make sure you use the compute height  to compute the number of rows to display.

- in the code : separate the editable DB management + db manager (the queue). from the Panel for edition. put then on separate files. Use similar conventions for name as the thumbnail manager (which deals with the thumbnail generating queue).
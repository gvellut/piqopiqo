- Extract the info panel from outside src/piqopiqo/fullscreen/overlay.py using an outside submodule in same fullscreen module
- Deal with event change + state (zoom level, direction, is small image). DEtermine inside that new submodule what and whether it must be shown. Also move should_show_zoom_overlay from zoom (not related to zoom itself ; just feedback on the zoom)
- also deal with one issue : when unzooming quickly : the 100% lingers on the base view. For others : it is replaced by the new zoom value so fine : for that particular case, since no display for base view : it is not hidden. First : so make sure it is hidden right away (not at the end of the timeout) when moving back to base view. Keep the showing rules for base images like now : just deal with the lingering (when the 100% is no longer true)
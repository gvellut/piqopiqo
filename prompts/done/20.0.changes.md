
- Add context menu right click : Regenerate thumbnail (for selected)
- Change the Regenerate Thumbnails menu from File to View : put it in its own section. 

- Add right click : Reveal in Finder : if multiple fiels selected and multiple folders : open multipe Finder windows with items selected in PiqoPiqo hightlighted. 
- Use python lib : show-in-file-manager or implement feature in specific file if not used

- Add right click : View in application and Edit in Application
- add config option in config.py : EXTERNAL_VIEWER : /Applications/Gimp.app (or possibly juste Gimp ?) + EXTERNAL_EDITOR
    - View in application : When context menu entry is selected : open the application by passing the path to the image as parameter or multiple paths 
    - Edit in application for Editing : same as above BUT first the file is duplicated (same). The path of the new file is used (instead of the original photo file)
- implement in specfic py file

- Add a File > Clear all data : with confirmation dialog => also clears the DB + cached thumbnails => Then after this is done, must reload the folder : reard the ExIF (to recreate the DB) + regeenrate the thumbnails

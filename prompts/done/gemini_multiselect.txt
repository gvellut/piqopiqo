- currently, in the grid, only one thumbnail can be selected by user
- Make it possible to also select multiple items
- Create a model for images : not just a string (path) but also other attributes, including selection status. When starting up, no item is selected. Put that class in new file model.py. You can use the attrs library.
- Use the usual keyboard modifiers (possibly supported directly by Qt) : for cmd (macos) or ctrl (other) to add to current selection (or remove if the clicked item is already selected) ; shift to add a range. Make it possible to have multiple steps : for example, select an item then cmd+click for another item, then shift + click to select the range starting from the last item selected : If the set of selected items included already some of the items in that range, keep them in the set of selected items (do not remove them ie revert the selection status). All of this should be pretty standard, there is no special behaviour.
- For the range, it should be by column first (or in other words : since the backing data is a list : simply the order of that list) : for example, select item in first column and row n , then shift+select item in first column and row n+1 : all the items in row n + last clicked item should be selected. NOT : just the 2 items in the first column => this would be incorrect.
- Handle multiselect to work with fullscreen view : currently after entering fullscreen, with a single item selected, arrows right or left pass through the whole set of thumbnails and it cycles back at the end or beginning of the set. Now also : When multiple items are selected, the arrows in fullscreen mode cycle through ONLY the selected items and cycle to the beginning / end ofr those items. The order is the position of the items in the backing data (NOT the order of when it was selected). When a single item is selected and fullscreen is entered, there should be no change in behaviour from now.
- Add an option in the config : when exiting the fullscreen, if multiple items were selected (and cycling through them), would exiting the fullscreen view : either keep the last viewed item as the only single selected item OR would all the items still be kept selected. Use a python enum for those 2 choices : create a model.py file with that Enum.  If the fullscreen was entered when a single item is selected (and cycling through the entire set of items), that config has no effect : when exiting, the last viewed item is selected, like it is the case currently.
- in the case of a single item is selected when exiting the fullscreen above, the grid should be scrolled and it should be bisible (just like now when only one is selected and exiting the fullscreen view : the selected item is visible)
- handle un-select for single items : currently, after one item is selected, it cannot be unselected. Make it possible using a click with the cmd / ctrl  modifier on that item. Then there is no visible selection and space keyboard shortcut as well as arrow left/right keybooard shortcut have no effect.
- Add a menu : Tools (to add)
And a submenu section : Save exif

This will recopy the metadatadb fields to Exif using exiftool through pyexiftools  for either the selected items or ALL the items that are currently filtered in (if none is selected)
The Exif tool to copy too are in EXIF_TO_DB_MAPPING in src/piqopiqo/metadata/db_fields.py
Use the ExifManger in src/piqopiqo/exif_man.py and process pool to perform the writing. Rename the functions so there is no confusion that the manager cna do multiple kinds of tasks : not just extracting, but also writing.
When Save exif is requested :
- open a dialog that will confirm the number of photos with Launch / Cancel at the bottom bar + a progress bar + non editable text box (both initially empty) => this will show the progress percent from the manager. 
- When launch has started : the exif is set to the files by the manager. The launch button is removed. The Cancel changes text and becomes Stop. If Stop is pressed : the exif manager is stopped (but there is no undo) and the dialog is closed.
- set data for each file from the DB data : you can use use the MWG flag for setting some of the values to multiple exif fields (lile title, description, keywords...). Add a configuraiont in db_fields.py for the mapping of a db field to MwG field or explicit field.
- The text box is updated with error logs from the exiftool process. It is scrollable
- the progress bar is updated with the percent of processed files : if there were errors for a file (ie not updated as indicated by the exiftool prcoess) : the progress bar is still incremented. However there is a message below it : x files with error
- When finish :  Button stop becomes OK. Clicking it closes the dialog
- When closing the dialog with OK or Stop :  If there are selected items in the grid : their exif should be reloaded (in case a visible field was changed by the exif saved). In any case, the next time an exif is requested for display in the exif panel, it should be reloaded (I think already the case).


Also set the 
Software agent: XMP:HistorySoftwareAgent
When: XMP:HistoryWhen
Action: XMP:HistoryAction

and

Software: XMP:ProcessingSoftware
Date: XMP:MetadataDate

Use Config.APP_NAME + v "version" (as read from the __init__.py) for the name and "now" for the date


- Add a __version__ = "1" to src/piqopiqo/__init__.py
+ way to read it from the application (for the processing / history )

Add 2 undo menu entry : subentries of an edit Menu (to add)
It is only one level-deep : it is not unlimited undo. However, multiple events can be aggregated 

It is for 2 cases :
- "Undo multiple edits": If editing data (with edit panel) : in src/piqopiqo/edit_panel.py : if multiple images were selected and 
This undo entry becomes active when . The undo structure / memory for that particularly edit type is clearead when. 
It is changed into Redo after the Undo has been called

- "Undo label": 

The "Undo multiple edits" can deal with the labels : that is, while multiple images are selected : 
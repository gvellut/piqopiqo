Arrange :
src/piqopiqo/exif_man.py
src/piqopiqo/exif_loader.py
src/piqopiqo/thumb_man.py

├── exif_loader.py   # EXIF metadata loading (background thread)
├── exif_man.py      # EXIF I/O manager (on-demand fetch + write to files)
├── thumb_man.py     # Thumbnail generation and caching (multiprocessing)

exif_loader and thumb_man should be in same (separate) submodule eg background : they provide a similar service : batch loading in the background. current exif_loader should be renamed exif_man
current exif_man.py should be renamed exif.py



launch batch image + exiftool : keep exitool process around
priority exiftool PreviewImage for all + exif ; full preview for visble priority : if not visible => after the PreviewImages
reorder queues (see last)
drop idle processes

exiftool : external process so threads : QThreadPool currently : see manage both


reorder the queue for thumbnails : if scrolling
double queues for preview + gen



Exiftool for reading in metadatadb => should be mutiprocessing (different processes). Take care of single writer for sqllite

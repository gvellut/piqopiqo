Search field in filter panel : Enter should return focus to photo grid. If an item is selected and is filtered in with the search term : a space bar after the search is submitted should allow the photogrid keypress handler to run and the fullscreen overlay to open.
Correct the other filters prompts/20.0.changes.md : Clicking on a checkbox for filters should return focus to the grid. The workflow : select an item in the grid, click on filter, space to open fullscreen does not work since the focus is not back to the grid after the click on filter
When doing Edit in ... (EXTERNAL_EDITOR processing cf config.py) : change selected item in grid to the newly duplicated files.

Add a Search button in filter panel next to search field : it will submit the current search

Copy the copy_sd feature in /Users/guilhem/Documents/projects/github/flickr_api_utils/flickr_api_utils/local.py line 547. Do not import that file. Create a new module copy_sd locally. You should use the config.py option BASE_EXTERNAL_FOLDER as the output_parent_folder in (local.py). Also do not use the MEDIA_FOLDER_MAPPING (in local.py) : instead use the name of the volume directly. To launch that feature, add a Copy from SD menu item in File. Open a dialog that shows similar text as the logging and click.confirm. When OK by the user, show a dialog with progress and cancel + OK button to close when finished.
[workspace]
members = [".", "uniffi-bindgen"]

[package]
name = "core_lib"
version = "0.1.0"
edition = "2024"

[lib]
name = "core_lib"
crate-type = ["cdylib", "staticlib"]

[dependencies]
uniffi = "0.29.4"

[build-dependencies]
uniffi = { version = "0.29.4", features = ["build"] }
